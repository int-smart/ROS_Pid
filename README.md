To launch the ROS node, open a terminal and start roscore. Now open a new 
terminal and navigate to the root directory of workspace and type 
source devel/setup.bash
roslaunch pid three_pid.launch

 
